from flask import Flask, render_template_string, request

app = Flask(__name__)

# أسعار الصرف المحدثة (مقابل 1 دينار تونسي)
EXCHANGE_RATES = {
    'USD': 3.12,   # دولار أمريكي
    'EUR': 3.35,   # يورو
    'TRY': 0.096,  # ليرة تركية
    'JPY': 0.020,  # ين ياباني
    'SAR': 0.83,   # ريال سعودي
    'AED': 0.85,   # درهم إماراتي
    'GBP': 3.90,   # جنيه إسترليني
    'CAD': 2.28,   # دولار كندي
    'QAR': 0.86    # ريال قطري
}

HTML_PAGE = '''
<!DOCTYPE html>
<html lang="fr" dir="ltr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Echange avec le Dinar Tunisien</title>
    <style>
        :root {
            --tunisian-red: #E70013;
            --white: #ffffff;
            --bg-light: #fdfdfd;
            --text-dark: #1a1a1a;
        }

        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            background: var(--bg-light); 
            margin: 0; 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh;
        }

        .container {
            background: var(--white);
            width: 100%;
            max-width: 400px;
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.06);
            text-align: center;
            border: 1px solid #f0f0f0;
            position: relative;
        }

        /* تصميم شعار E مع خطين */
        .logo-box {
            width: 85px;
            height: 85px;
            background: var(--tunisian-red);
            margin: 0 auto 25px;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            box-shadow: 0 8px 20px rgba(231, 0, 19, 0.2);
        }

        .logo-e {
            color: white;
            font-size: 55px;
            font-weight: 900;
            position: relative;
            font-family: 'Georgia', serif;
        }

        /* الخطين المتقاطعين على حرف E */
        .logo-e::before {
            content: "";
            position: absolute;
            left: -8px;
            top: 40%;
            width: 55px;
            height: 4px;
            background: white;
            box-shadow: 0 12px 0 white;
        }

        h1 {
            color: var(--tunisian-red);
            font-size: 1.5rem;
            margin-bottom: 25px;
            font-weight: 800;
            letter-spacing: -0.5px;
        }

        .hero-img {
            width: 100%;
            height: 150px;
            background: url('https://images.unsplash.com/photo-1555939594-58d7cb561ad1?q=80&w=1000&auto=format&fit=crop') center/cover;
            border-radius: 16px;
            margin-bottom: 25px;
            border: 1px solid #eee;
        }

        .input-group {
            text-align: left;
            margin-bottom: 20px;
        }

        label {
            display: block;
            font-size: 0.9rem;
            font-weight: 700;
            color: #4a5568;
            margin-bottom: 8px;
            padding-left: 4px;
        }

        input, select {
            width: 100%;
            padding: 14px;
            border: 2px solid #edf2f7;
            border-radius: 12px;
            font-size: 1rem;
            box-sizing: border-box;
            background: #fafafa;
            transition: all 0.3s ease;
        }

        input:focus {
            border-color: var(--tunisian-red);
            outline: none;
            background: white;
            box-shadow: 0 0 0 4px rgba(231, 0, 19, 0.05);
        }

        .convert-btn {
            width: 100%;
            padding: 16px;
            background: var(--tunisian-red);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 10px;
            box-shadow: 0 10px 20px rgba(231, 0, 19, 0.15);
        }

        .convert-btn:hover {
            background: #c60011;
            transform: translateY(-2px);
        }

        .result-box {
            margin-top: 25px;
            padding: 20px;
            background: #fff8f8;
            border-radius: 18px;
            border: 1px dashed var(--tunisian-red);
            animation: fadeIn 0.4s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .res-title {
            font-size: 0.85rem;
            color: #718096;
            margin-bottom: 6px;
        }

        .res-value {
            font-size: 2.2rem;
            font-weight: 900;
            color: var(--tunisian-red);
        }

        .footer {
            margin-top: 30px;
            font-size: 0.8rem;
            color: #a0aec0;
            border-top: 1px solid #f1f1f1;
            padding-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Logo حرف E مع الخطين -->
        <div class="logo-box">
            <span class="logo-e">E</span>
        </div>

        <h1>Echange avec le Dinar Tunisien</h1>

        <!-- صورة الخلفية -->
        <div class="hero-img"></div>

        <form method="POST">
            <div class="input-group">
                <label>Montant en (TND)</label>
                <input type="number" step="0.001" name="amount" placeholder="0.000" required value="{{ amount }}">
            </div>

            <div class="input-group">
                <label>Vers la devise</label>
                <select name="currency">
                    <option value="USD" {% if currency == 'USD' %}selected{% endif %}>🇺🇸 Dollar US (USD)</option>
                    <option value="EUR" {% if currency == 'EUR' %}selected{% endif %}>🇪🇺 Euro (EUR)</option>
                    <option value="TRY" {% if currency == 'TRY' %}selected{% endif %}>🇹🇷 Lire Turque (TRY)</option>
                    <option value="JPY" {% if currency == 'JPY' %}selected{% endif %}>🇯🇵 Yen Japonais (JPY)</option>
                    <option value="SAR" {% if currency == 'SAR' %}selected{% endif %}>🇸🇦 Riyal Saoudien (SAR)</option>
                    <option value="AED" {% if currency == 'AED' %}selected{% endif %}>🇦🇪 Dirham Émirati (AED)</option>
                    <option value="GBP" {% if currency == 'GBP' %}selected{% endif %}>🇬🇧 Livre Sterling (GBP)</option>
                </select>
            </div>

            <button type="submit" class="convert-btn">CALCULER L'ÉCHANGE</button>
        </form>

        {% if result %}
        <div class="result-box">
            <div class="res-title">Montant converti :</div>
            <div class="res-value">{{ result }} {{ currency }}</div>
        </div>
        {% endif %}

        <div class="footer">
            Design by Adam | Moknine, Tunisie &copy; 2026
        </div>
    </div>
</body>
</html>
'''

@app.route('/', methods=['GET', 'POST'])
def index():
    result = None
    amount = ''
    currency = 'USD'
    
    if request.method == 'POST':
        try:
            amount_val = float(request.form['amount'])
            currency = request.form['currency']
            rate = EXCHANGE_RATES.get(currency, 1)
            # التحويل من دينار إلى عملة أجنبية
            converted = amount_val / rate
            result = "{:,.2f}".format(converted)
            amount = amount_val
        except:
            result = "Erreur"
            
    return render_template_string(HTML_PAGE, result=result, amount=amount, currency=currency)

if __name__ == '__main__':
    app.run(debug=True)